frontend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist    # <-- Update this line
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*